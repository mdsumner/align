% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vast.R
\name{vast}
\alias{vast}
\title{Create a grid specification}
\usage{
vast(dimension, extent = NULL)
}
\arguments{
\item{dimension}{integer vector \code{c(ncol, nrow)}}

\item{extent}{numeric vector \code{c(xmin, xmax, ymin, ymax)}, or \code{NULL} for
unit cell space}
}
\value{
A \code{vast} object (named list with class)
}
\description{
A grid specification defined by dimension and extent. Resolution,
coordinates, cell indices, and row/column mappings are derived on demand.
}
\details{
Dimension is \code{c(ncol, nrow)}. Extent is \code{c(xmin, xmax, ymin, ymax)}.

When \code{extent} is \code{NULL}, defaults to unit cell space: \code{c(0, ncol, 0, nrow)}.
This gives an identity mapping where column index = x coordinate and row
index = y coordinate.
}
\examples{
vast(c(100L, 200L))
vast(c(500L, 300L), extent = c(140, 160, -50, -30))
}
